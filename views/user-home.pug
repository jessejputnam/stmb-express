extends layout 

block content 
  h1= title 

  p This is the page of #{currentUser.firstname}
  p(style="margin-bottom:20px")= currentUser.username
  p Creator: #{currentUser.creator}
  p Page: #{currentUser.pageId}
  p Region: #{currentUser.region}
