extends layout 

block content
  div.page-wrapper
    h1= title 

    div 
      ul 
        each page in page_list 
          li 
            a(href=page.url)= page.title 
              span(style="color:gray;font-size:.7rem")  (#{page.genre.type})