extends layout 

block content
  div.page-wrapper
    h1= title 

    div 
      ul 
        each creator in creator_list 
          li 
            a(href=creator.url)= creator.name 
              span (#{creator.genre.type})